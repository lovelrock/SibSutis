CMAKE
1)./bootstrap --prefix=$HOME/cmake
2)make -j$(nproc)
3)make install
4)export PATH="$HOME/cmake/bin:$HOME/mold_install/bin:$PATH"
MOLD
1)git clone --branch stable https://github.com/rui314/mold.git .
2)git checkout на тэг git tag -l
3)cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$HOME/mold_install -B build
4)cmake --build build -j$(nproc) // неявный вызов make
5)sudo cmake --build build --target install
6)export PATH="$HOME/mold_install/bin:$PATH"


проверка что после сборки использовался линкер 
readelf -p .comment bin/clang | grep mold


https://wiki.csc.sibsutis.ru/en/ClusterPine
ssh username@pine.cpct.sibsutis.ru
https://github.com/kitware/cmake


cd ~/llvm-project
mkdir build-default
cd build-default
cmake -DCMAKE_BUILD_TYPE=Release \
      -DLLVM_ENABLE_PROJECTS="clang;lld" \
      -DCMAKE_INSTALL_PREFIX=$HOME/llvm_install \
      ../llvm
time make -j$(nproc)


